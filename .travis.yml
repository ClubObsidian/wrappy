language: java
dist: trusty
sudo: false

before_install:
- chmod +x gradlew

os:
- linux

jdk:
- oraclejdk8
- openjdk8
- oraclejdk9
- openjdk9
- openjdk10
- openjdk11


script:
- echo PATH = ${PATH}
- echo JAVA_HOME = ${JAVA_HOME}
- java -version
- ./gradlew test
- ./gradlew jacocoTestReport
after_success:
- bash <(curl -s https://codecov.io/bash)